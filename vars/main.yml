---
# vars file for alternatives
_alternatives_packages:
  default:
    - alternatives
  Alpine:
    - update-alternatives
  CentOS:
    - chkconfig
  Debian:
    - dpkg
  Ubuntu:
    - dpkg

alternatives_packages: "{{ _alternatives_packages[ansible_distribution] | default(_alternatives_packages['default']) }}"
